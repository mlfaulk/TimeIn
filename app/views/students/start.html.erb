<div class="student-background">
	<div>
		<a href="chill">
			<p class="title motto blue-text" id="start_btn" >START</p>
		</a>
	</div>


	<a href="#login-box" class="login-window">Login / Sign In</a>

	<div id="login-box" class="login-popup">
		<div class="teacher-icon">
	        <%= image_tag("teacher_icon.png") %>
		</div>
		<p class="small-txt">Teacher Login</p>
		<a href="#" class="close">
	        <%= image_tag("close_pop.png", class: "btn_close", alt:"X") %>
	    </a>
	    <%= form_tag("teacher_login", method: "post", class:"signin") do %>

	  <!--<form method="post" class="signin" action="#">-->
	  	<div id="formholder">
	        <%= text_field_tag(:username, "Username")%>
	        <!--<input id="username" name="username" value="" type="text" placeholder="Username">-->
	        <input id="password" name="password" value="" type="password" placeholder="Password">
	        <!--<button class="submit button-login" type="button">Sign in</button>-->
	        <p class="submit rounded orange-text"><%= submit_tag("Sign In") %></p>

	        <p class="small-txt">
	            <a class="forgot" href="#">Forgot your password?</a>
	        </p>        
	    </div>
	    <% end %>

	  <!--</form>-->
	</div>

	<script type="text/javascript">
		$(document).ready(function() {
		$('a.login-window').click(function() {
		    
		    //Getting the variable's value from a link 
		    var loginBox = $(this).attr('href');

		    //Fade in the Popup
		    $(loginBox).fadeIn(300);
		    
		    //Set the center alignment padding + border see css style
		    var popMargTop = ($(loginBox).height() + 24) / 2; 
		    var popMargLeft = ($(loginBox).width() + 24) / 2; 
		    
		    $(loginBox).css({ 
		        'margin-top' : -popMargTop,
		        'margin-left' : -popMargLeft
		    });
		    
		    // Add the mask to body
		    $('body').append('<div id="mask"></div>');
		    $('#mask').fadeIn(300);
		    });

		    // When clicking on the button close or the mask layer the popup closed
		    $('a.close, #mask').live('click', function() { 
		        console.log("Close here")
		      $('#mask , .login-popup').fadeOut(300 , function() {
		        $('#mask').remove();  
		       }); 
		      return false;
		    });
		});
</script>
</div>


